// 조팸스 GO v5 · Cloudflare Workers + D1 랭킹 API
// D1 binding: DB. 실시간 GPS/카메라/센서 원본값은 받지 않습니다.
const json=(data,status=200,headers={})=>new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json;charset=UTF-8',...headers}});
export default {async fetch(req,env){const u=new URL(req.url);const allow=env.ALLOWED_ORIGIN||'*';const h={'access-control-allow-origin':allow,'access-control-allow-methods':'GET,POST,OPTIONS','access-control-allow-headers':'content-type','cache-control':'no-store'};if(req.method==='OPTIONS')return new Response(null,{status:204,headers:h});if(u.pathname==='/health')return json({ok:true,service:'jopams-go-ranking',version:'v5'},200,h);if(u.pathname==='/api/score'&&req.method==='POST'){let b;try{b=await req.json()}catch{return json({ok:false,error:'invalid_json'},400,h)}const name=String(b.name||'원정대원').replace(/[<>]/g,'').trim().slice(0,30)||'원정대원',org=String(b.org||'본사').replace(/[<>]/g,'').trim().slice(0,30)||'본사',score=Math.min(99999999,Math.max(0,Math.floor(Number(b.score)||0)));await env.DB.prepare('INSERT INTO scores(name,org,score,updated_at) VALUES(?,?,?,CURRENT_TIMESTAMP) ON CONFLICT(name,org) DO UPDATE SET score=MAX(score,excluded.score),updated_at=CURRENT_TIMESTAMP').bind(name,org,score).run();return json({ok:true},200,h)}if(u.pathname==='/api/leaderboard'&&req.method==='GET'){const {results}=await env.DB.prepare('SELECT name,org,score,updated_at FROM scores ORDER BY score DESC,updated_at ASC LIMIT 100').all();return json({items:results||[]},200,h)}return json({ok:false,error:'not_found'},404,h)}}